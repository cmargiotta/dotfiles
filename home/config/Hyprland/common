general {
  main_mod = SUPER

  gaps_in = 2
  gaps_out = 8
  border_size = 2
  col.active_border = 0x66ee1111
  col.inactive_border = 0x66333333
  layout = master

  damage_tracking = full
}

decoration {
  rounding = 10
  blur = 1
  blur_size = 3 # minimum 1
  blur_passes = 1 # minimum 1
  blur_new_optimizations = 1
  inactive_opacity = 0.9
  drop_shadow = true
  shadow_range = 3
  multisample_edges = true
}

animations {
  enabled = 1
  animation = windows,1,7,default
  animation = border,1,10,default
  animation = fade,1,10,default
  animation = workspaces,1,6,default
}

input {
  kb_layout = us
  kb_variant = intl
}

master {
  no_gaps_when_only = true
  new_is_master = false
  new_on_top = true
}

exec-once = waybar
exec-once = hash dbus-update-activation-environment 2>/dev/null && dbus-update-activation-environment DISPLAY WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

bind = SUPER,Return,exec,kitty
bind = SUPER,D,exec,wofi

bind = SUPER,Q,killactive
bind = SUPER,F,fullscreen
bind = SUPERSHIFT,F,togglefloating
bind = SUPER,M,layoutmsg,swapwithmaster

bind = SUPER,left,movefocus,l
bind = SUPER,right,movefocus,r
bind = SUPER,up,movefocus,u
bind = SUPER,down,movefocus,d

bind = SUPERSHIFT,left,movewindow,l
bind = SUPERSHIFT,right,movewindow,r
bind = SUPERSHIFT,up,movewindow,u
bind = SUPERSHIFT,down,movewindow,d

bind = ,Print,exec,grimblast copy output
bind = SUPER,S,exec,grimblast copy area
bind = SUPERSHIFT,S,exec,grimblast copy window

bind = SUPER,1,workspace,1
bind = SUPER,2,workspace,2
bind = SUPER,3,workspace,3
bind = SUPER,4,workspace,4
bind = SUPER,5,workspace,5
bind = SUPER,6,workspace,6
bind = SUPER,7,workspace,7
bind = SUPER,8,workspace,8
bind = SUPER,9,workspace,9
bind = SUPER,0,workspace,10

bind=SUPERSHIFT,1,movetoworkspace,1
bind=SUPERSHIFT,2,movetoworkspace,2
bind=SUPERSHIFT,3,movetoworkspace,3
bind=SUPERSHIFT,4,movetoworkspace,4
bind=SUPERSHIFT,5,movetoworkspace,5
bind=SUPERSHIFT,6,movetoworkspace,6
bind=SUPERSHIFT,7,movetoworkspace,7
bind=SUPERSHIFT,8,movetoworkspace,8
bind=SUPERSHIFT,9,movetoworkspace,9
bind=SUPERSHIFT,0,movetoworkspace,10

bind=ALTSHIFT,down,movecurrentworkspacetomonitor,d
bind=ALTSHIFT,up,movecurrentworkspacetomonitor,u
bind=ALTSHIFT,left,movecurrentworkspacetomonitor,l
bind=ALTSHIFT,right,movecurrentworkspacetomonitor,r

bind = SUPERSHIFT,R,exec,hyprctl reload

windowrule=float,pavucontrol-qt